{
  "build": {
    "builder": "NIXPACKS", 
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "node back/dist/main"
  },
  "environment": {
    "providers": ["node"] 
  }
}
